import 'package:flutter/material.dart';

class ExercisesGraph extends StatelessWidget {
  const ExercisesGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Exercises Graph Placeholder',
        style: Theme.of(context).textTheme.bodyLarge,
      ),
    );
  }
}
